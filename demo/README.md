# Demo mode

Set `DEMO_MODE=true` to make the application run without a camera. The demo feed uses generated workpiece-like frames and stores results with `is_demo=1`; demo records are visibly labeled in the dashboard and are separate from real camera records.